import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  itemContainer: {
    marginTop: 10,
  },
  emptyList: {
    marginTop: 10,
    textAlign: 'center',
    fontSize: 16,
  },
});

export {styles};
